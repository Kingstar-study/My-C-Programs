/* Author  : Rizwan
   Problem : Calculate the area of a circle and modify the same program to calculate the
volume of a cylinder given its radius and height. */

#include <stdio.h>

int main()
{
    float c = 100;
    float f = (c * 9 / 5) + 32;
    printf("%f Celsius is equal to %f Fahrenheit", c, f);
    return 0;
}